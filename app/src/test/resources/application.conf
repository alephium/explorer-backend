db = {
  profile = "slick.jdbc.PostgresProfile$"
  db {
    connectionPool = disabled
    driver   = "org.postgresql.Driver"
    host     = "localhost"
    user     = "postgres"
    password = "postgres"
    properties = {
      prepareThreshold = 1
    }
  }
}
